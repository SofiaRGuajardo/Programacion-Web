<nav class="navbar navbar-dark text-white" style="background-color: #0B005D;">
  <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <a class="navbar-brand">
          <img src="/images/logo/logo_small_icon_only_inverted.png" alt="" width="80px">
        </a>
        <h4>Hola {{usuario}}</h4>
      </div>
       <div class="d-flex justify-content-end">
          <a href="/admin/novedades/agregar" class="btn btn-light me-3" style="background-color: #6cec3a;"><i class="fa fa-plus" aria-hidden="true"></i> Nuevo</a>
          <a href="/" class="btn btn-light me-3"><i class="fa fa-home" aria-hidden="true"></i> Inicio</a>
          <a href="/admin/login/logout" class="btn btn-danger"><i class="fa fa-reply" aria-hidden="true"></i> Salir</a>
        </div>
  </div>
</nav>

<div class="container" style="margin-top: 80px;">
  <div class="row">
    <div class="col-6">
    <h2>NOVEDADES</h2>
     </div>
    <div class="col-6">
        <form class="d-flex" action="/admin/novedades" method="get">
        <input class="form-control me-2" type="search" placeholder="Buscar" aria-label="Search" name="q" {{#if q}}value="{{q}}" {{/if}}>
        <button class="btn btn-outline-success" type="submit">Buscar</button>
      </form>
    </div>
  </div>

<!--TABLA-->
  <div class="row">
      <div class="col">
        <table class="table table-striped table-sm">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">Fecha</th>
              <th scope="col">Lugar</th>
              <th scope="col">Título</th>
              <th scope="col">Acciones</th>
            </tr>
          </thead>
          <tbody>
            {{#each novedades}}
            <tr>
              <th scope="row">{{id}}</th>
              <td>{{fecha}}</td>
              <td>{{ubicacion}}</td>
              <td>{{titulo}}</td>
              <td>
                <a href="/admin/novedades/modificar/{{id}}" class="btn btn-sm btn-secundary">
                  <i class="fa fa-pencil"></i></a>
                <a href="/admin/novedades/eliminar/{{id}}" class="btn btn-sm btn-secundary">
                  <i class="fa fa-trash"></i></a>
              </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
        {{#if is_seach}}
        {{#unless novedades.length}}
        No se encontraron resultados de "<b>{{q}}</b>"
        {{/unless}}
        {{/if}}
      </div>
    </div>
</div>